const isNull = x => x === null;
const hasNoLength = x => x.length === 0;
const isEmpty = x => x === "";
const isUndefined = x => x === undefined;

export { isEmpty, isNull, hasNoLength, isUndefined };
